{
  "name": "@minecraft-ui/monorepo",
  "private": true,
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "build": "lerna run build",
    "test": "lerna run test",
    "lint": "lerna run lint",
    "dev": "lerna run dev --parallel",
    "storybook": "cd apps/core-demo && npm run start",
    "release": "lerna version && lerna publish"
  },
  "devDependencies": {
    "@typescript-eslint/eslint-plugin": "^6.0.0",
    "@typescript-eslint/parser": "^6.0.0",
    "concurrently": "^8.0.0",
    "copyfiles": "^2.4.1",
    "eslint": "^8.0.0",
    "lerna": "^7.4.2",
    "prettier": "^3.0.0",
    "rimraf": "^5.0.0",
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "@minecraft-ui/core": "^1.0.0",
    "rollup-plugin-copy": "^3.5.0"
  }
}
